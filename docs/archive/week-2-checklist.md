# Week 2 Checklist

Week 2 status: make the foundation executable.

## Done Criteria

- [x] Tooling choices are documented.
- [x] Contract examples are validated against JSON Schemas.
- [x] OpenAPI files parse as YAML.
- [x] Docker Compose config validates for default services.
- [x] Docker Compose config validates for future service placeholders.
- [x] Docker Compose config validates for sandbox placeholder.
- [x] PostgreSQL migration exists and is mounted into Compose.
- [x] Ollama/Kali VM configuration is represented through `.env.example`.

## Deferred Until Service Implementation

- [ ] Java services build locally after JDK 21 and Maven are installed.
- [ ] Python services run after dependencies are installed in service environments.
- [ ] Dashboard runs after npm dependencies are installed.
