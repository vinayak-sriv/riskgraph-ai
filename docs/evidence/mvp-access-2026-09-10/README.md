# Continuation evidence bundle

Captured on 10 September 2026 after live Ollama, platform accounts and external-source
checks. This supplements the earlier `mvp-2026-09-10` baseline.

- Four scenario scans, `validated.json` and `summary.json`: real immutable source
  pairs, deterministic repeated evidence, live AI for findings and Docker confirmation.
- `live-ollama.json`: installed model digest, both schema-constrained AI routes,
  permitted HTTP proposal and unchanged deterministic source result.
- `platform-access.json`, `runtime-checks.json`: role restrictions, CSRF, logout,
  persisted accounts, platform restart roundtrip and normalized validation consistency.
- `external-spring/`: both pinned source results, including the duplicate-type parser
  failure, and honest partial extraction metrics. Labels remain provisional.
- `quality-gate.json`: all nine checks passed; 110 tests in the full suite.
- `corpus-evaluation.json`: provisional synthetic regression metrics.
- `check.json`, `results.sarif`, `summary.md`: offline GitHub reports; no publication.
- `browser-checks.json`: observed login, Analyst access, saved-scan and layout checks.
- `protected-validation.json`: final protected revision HTTP 403 / REJECTED, image
  and commit binding, and complete cleanup after Docker restart.

No credentials are included. Full results, important changes, reproduction commands
and limits are in `docs/continuation-report-2026-09-10.md`.
