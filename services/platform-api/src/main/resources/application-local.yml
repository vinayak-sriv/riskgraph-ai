# Explicit ephemeral demo mode; the default profile requires PostgreSQL.
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
riskgraph:
  demo:
    dynamic-enabled: ${RISKGRAPH_DYNAMIC_DEMO_ENABLED:true}
